#include "sejf.h"
#include <cassert>
#include <sstream>

using namespace std;

/* Test na domyślną liczbę prób. */

int main()
{
	stringstream ss;
	Sejf s1("aaa");
	s1 -= 41;
	assert(s1[0] == 'a');
	assert(s1[0] == -1);
}
